import { createOrganization } from "./actions";
import { AuthSubmitButton } from "@/components/auth-submit-button";
import { createClient } from "@/lib/supabase/server";
import { redirect } from "next/navigation";
export default async function Onboarding({searchParams}:{searchParams:Promise<{error?:string}>}) { const supabase=await createClient(); const {data:{user}}=await supabase.auth.getUser(); if(!user) redirect('/login'); const {data:memberships}=await supabase.from("organization_memberships").select("id").eq("user_id",user.id).eq("membership_status","active").limit(1); if(memberships?.length) redirect('/'); const {error}=await searchParams; return <main className="auth-page"><form action={createOrganization} className="auth-card"><div className="auth-brand"><span className="brand-mark">L</span><b>Ledgerly</b></div><h1>Set up your company</h1><p>Start with your company and primary branch. Currency is AED and timezone is Asia/Dubai.</p><label>Company name<input name="name" required /></label><label>Legal name <input name="legal" /></label><label>Branch name<input name="branch" required /></label>{error&&<p className="error">{error}</p>}<AuthSubmitButton idle="Create company" pending="Creating company…" /></form></main> }
